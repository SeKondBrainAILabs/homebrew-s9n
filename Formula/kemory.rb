# This file is generated by the kemory release-cli workflow. Do not edit by hand.
class Kemory < Formula
  desc "Persistent memory CLI for AI agents — browser sign-in, MCP bridge"
  homepage "https://github.com/SeKondBrainAILabs/kemory"
  version "0.6.5"
  license "MIT"

  # Separate per-arch archives (no universal2 — see the build matrix).
  # Intel support depends on the macos-15-intel runner, which GitHub
  # retires in August 2027; see the matrix comment before removing.
  on_macos do
    on_arm do
      url "https://github.com/SeKondBrainAILabs/homebrew-s9n/releases/download/cli-v0.6.5/kemory-macos-arm64.tar.gz"
      sha256 "d2817ccd26e42e1e89dff546d6a642738f0204fb345c2b7a45de38d144e9d082"
    end
    on_intel do
      url "https://github.com/SeKondBrainAILabs/homebrew-s9n/releases/download/cli-v0.6.5/kemory-macos-x64.tar.gz"
      sha256 "042d359c61985fbfe315f65fb1baf7a1767e9cbbb7e5973805bc19f1bbd1d3fc"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/SeKondBrainAILabs/homebrew-s9n/releases/download/cli-v0.6.5/kemory-linux-arm64.tar.gz"
      sha256 "186c979baff93d60610629c2a0bb5cb613aca7a6251f5a48886dee840eb3a5a0"
    end
    on_intel do
      url "https://github.com/SeKondBrainAILabs/homebrew-s9n/releases/download/cli-v0.6.5/kemory-linux-x64.tar.gz"
      sha256 "55c89c8e70fb9dc883cc36e76ec971fef32fbdaadaf99b00ad2863fb18df0419"
    end
  end

  livecheck do
    url "https://github.com/SeKondBrainAILabs/homebrew-s9n"
    strategy :github_latest
  end

  def install
    libexec.install Dir["*"]
    bin.install_symlink libexec/"kemory"

    # Shell completions. Generated by running the freshly-installed
    # binary, so they always match the commands this build actually
    # has — a checked-in copy would drift the moment one is added.
    generate_completions_from_executable(
      libexec/"kemory", "completion", shell_parameter_format: :arg
    )
  end

  test do
    assert_match "version", shell_output("#{bin}/kemory --version")
    # Proves the [tui] extra survived the freeze: the browse
    # command only exists when kemory_cli.tui imports, which is
    # what breaks in a binary missing textual .tcss data files.
    # No credentials in the brew sandbox, so this exits non-zero
    # either way — what differs is WHICH failure. "log in first"
    # means the tui import succeeded; the install hint means the
    # extra did not survive the freeze.
    refute_match "optional TUI dependency",
                 shell_output("#{bin}/kemory browse 2>&1", 3)
  end
end
